import assert from 'assert'
describe('Test setup', () => {
    it('should find the correct artist', () => {
        let artist: string = 'Illenium'
        assert(typeof artist === "string", 'you don\'t know ts, do you?')
    })
})